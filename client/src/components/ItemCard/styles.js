import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    card: {
      minWidth: 350,
      minHeight: 400
    },
    media: {
      height: 140
    }
  });

export default styles;
